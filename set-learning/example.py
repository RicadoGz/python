"""
 set.add(element)

向集合中添加一个元素。


set.remove(element)

移除集合中指定的元素，若不存在会抛出错误。


set.pop()

从集合中随机取出一个元素（集合无序）。
set.clear()

将集合清空，删除所有元素。
set1.difference(set2)

返回一个新集合，包含 set1 中有但 set2 中没有的元素（差集）。
原集合不变。
set1.difference_update(set2)

从 set1 中移除 set2 存在的元素，直接修改 set1。
set1 被改变，set2 不变。
set1.union(set2)

返回一个新集合，包含 set1 和 set2 的所有元素（并集）。
原集合不变。
len(set)

返回集合中元素的个数。

"""

""